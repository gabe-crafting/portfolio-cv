{
  "name": "portfolio-cv",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Personal CV and website (static HTML/CSS) with a PDF export script.",
  "scripts": {
    "serve": "serve . -l 3000",
    "pdf": "node scripts/generate-pdf.js"
  },
  "devDependencies": {
    "puppeteer": "^23.0.0",
    "serve": "^14.2.3"
  }
}
