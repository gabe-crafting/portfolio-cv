# Portfolio CV

Personal CV site — plain HTML and CSS. Open `index.html` in a browser, or serve locally:

```bash
npx serve .
```

Edit content directly in `index.html` and styling in `styles.css`. No build step required.
